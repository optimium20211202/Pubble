type Props = {
  params: {
    topicId: string;
  };
};
export default function RecommendationTopicPage({
  params: { topicId },
}: Props) {
  return <div>📰 ターゲットがみたニュース</div>;
}
